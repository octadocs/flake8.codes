# Show violations marked as related to this one.
# Accepts: ?this - IRI of the current violation page.
SELECT * WHERE {
    ?this :relatedToViolation [
        :internalName ?name ;
        :code ?code ;
        octa:title ?title ;
        octa:url ?url
    ] .
} ORDER BY ?code
